# Phase4 remaining discovery and speed corrections

Pre-execution,13 September2026. Engine56e1d86/0.115.1 and permanent browser002 evidence.

## Evidence and cause

Exact arrow query in the preserved Luna chat retrieved two stale tool results via `read`, made zero current ascent calls, and listed only Ordinary despite the live six-payload quiver. Explicit ascent_character refresh correctly listed all six. Earlier instructions on generic scene/character tools are insufficient for this narrow question. The actual drawer, Fire/Arcane/Poison, source timing, push and invalid/missed/empty ammunition browser checks passed.

The real floor80 Tower Gate lists floor names and town/party hints but omits its resource place; only the floor camp advertises Mythic Loom Hollow. The user explicitly requested site discovery at the Gate.

Resolver inspection after Frostbind niche measurement shows three different speed uses: pursuit subtracts slow, but withdrawal and escape use unmodified monster speed. Active slow therefore fails to help those two speed-dependent choices and the public speed remains stale. Targeted no-shoes Air tests already show Frostbind can help pursuit (52wins versus33plain in64matched pairs), so preserve its unequal niche rather than increasing all damage.

## Changes

1. Add a small dedicated current-ammunition tool and corresponding collection Arrows view action. It must refresh actual live stock, six definitions/quotes and resolved bow payload, then show the real Arrows drawer without buying/firing/spending. Its name/description explicitly target arrow questions and distinguish current live data from historical tool-result retrieval. Preserve general character/scene tools and old clients.
2. Add actual site names/target materials to candidate Gate floor hints using gathering.SITES; camp/tool access remains unchanged. Format long late-site prices with separators and keep mobile wrapping.
3. Centralize effective monster speed while slow is active. Use it for pursuit, withdrawal, escape and displayed current speed. Compute it before decrementing the status for the current action; maintain two-phase expiration and no ticks on read/free selection/refusal. Preserve old combat_revision1 behavior through groups_v1; do not rewrite persisted effects.
4. Exact tests for slow's first/second/expired movement and escape threshold, immutable reads, separate legacy resolver, gate sites and ammunition display without spend. Focused then full tests, matching vendor/wiki; restart only owned QA services with current databases. Recheck exact arrow question plus safe free-preview selection/leave on the earned20Arcane, Gate/mobile hints, and slowed withdrawal in browser.

## Separate tracked failure

P3-008/P4-004: Luna made five choices under a four-action cap twice. This is not corrected by another prose change. Phase7 requires a deterministic per-user-turn choice allowance using sanctioned conversation context and backend receipts, including restart/retry/multi-conversation tests. Preserve this failure and the natural earned state. No extra combat is authorized merely to rerun it now.

## Rollback

Record implementation SHAs and verification before advancing. Revert these discovery/display additions and retain all0.115 arrow/effect readers; old saved groups keep their pinned resolver. For a speed regression, retain current saved slow counters and fix forward or drain affected QA groups; do not replay spent actions or overwrite characters. Snapshot before restarting only recorded processes. No production change.

Execution status,13 September2026: implemented in0.115.2.67 focused tests pass, including exact two-phase speed expiry, a matched20% escape roll, immutable drawer reads and all8 Gate sites. Full suite:1539passed,8previously tracked failures,4skipped,1xfail in19.81seconds. No new failure. Dedicated tool registration and first-query discovery still require restarted real Luna. Earlier browser002 evidence remains unchanged; phase not complete.

Rollback: revert the implementation commit following0924336, retain owned quiver/effect documents and pinned groups_v1 reader. Regenerate matching vendor/wiki. Restart only owned local QA services after durable database snapshots. No production operation.
